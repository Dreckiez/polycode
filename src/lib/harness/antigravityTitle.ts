import type { GeneratedSessionTitle } from "../sessionTitle";
import type { TitleInput } from "./registry";

export async function generateAntigravitySessionTitle(
  _input: TitleInput,
): Promise<GeneratedSessionTitle | null> {
  return null;
}
